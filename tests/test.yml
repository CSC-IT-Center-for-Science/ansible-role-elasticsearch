---
- hosts: test.lxc
  remote_user: root
  become: True
  roles:
    - ansible-role-elasticsearch
  vars:
    - elasticsearch_install_version: "5.x"
    - elasticsearch_extra_settings:
      - "node.data: true"

# vim:ft=ansible:
